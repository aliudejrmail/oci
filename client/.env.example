# URL base da API (opcional).
# Se não definir, o Vite usa o proxy para /api (localhost:3001).
# Use quando o proxy falhar ou para apontar a outro host/porta:
# VITE_API_URL=http://localhost:3001/api
